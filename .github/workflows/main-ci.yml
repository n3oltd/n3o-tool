name: Main CI

on:
  push:
    branches: main
    paths:
      - 'src/**'

  workflow_dispatch:

jobs:
  build:
    name: Build
    uses: n3oltd/actions/.github/workflows/dotnet-build-pack-push.yml@main
